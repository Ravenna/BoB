<h1>My Recommendations</h1>


<% @recommendations.each do |rec| %>
	<h3>
		<% if rec.approvals.count == 0 %>
			<%= link_to rec.nominee, edit_category_award_recommendation_path(@category, rec.award, rec) %> </h3>
		<% else %>
			<%= rec.nominee %> (Submitted)
		<% end %>
<% end %>